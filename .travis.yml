language: generic

sudo: required

services:
  - docker

before_install:
  - docker build -t gks/react-test-app -f ./frontend/Dockerfile.dev ./frontend

script:
  - docker run -e CI=true gks/react-test-app npm run test

after_success:
  - docker build -t gks/docker-frontend ./frontend
  - docker build -t gks/docker-backend ./backend
  - docker build -t gks/docker-nginx ./nginx

  - echo "$DOCKER_HUB_PASSWORD" | docker login -u "$DOCKER_HUB_ID" --password-stdin

  - docker push gks/docker-frontend
  - docker push gks/docker-backend
  - docker push gks/docker-nginx

# deploy:
#   provider: elasticbeanstalk
#   region: "ap-northeast-2"
#   app: "docker-fullstack-app"
#   env: "DockerFullstackApp-env"
#   bucket_name: elasticbeanstalk-ap-northeast-2-972153559337
#   bucket_path: "docker-fullstack-app"
#   on:
#     branch: main
  
#   access_key_id: $AWS_ACCESS_KEY
#   secret_access_key: $AWS_SECRET_ACCESS_KEY